{
    "private": true,
    "type": "module",
    "scripts": {
        "install": "composer install",
        "setup": "cp .env.example .env && php artisan key:generate && php artisan migrate",
        "dev": "php artisan serve",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
